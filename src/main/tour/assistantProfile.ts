/**
 * 应用背景知识库（挂载给大模型回答听众提问）
 *
 * 听众问到"这个助手本身"相关问题（能做什么、怎么用、数据是否安全等）时，
 * 把这些事实作为参考注入 LLM，保证回答与本应用实际行为一致。
 *
 * 内容随界面语言切换：英文界面下助手用英文作答，注入的背景知识也必须是英文，
 * 否则模型会照着中文资料用中文回答。故此处不导出常量而导出取词函数。
 */

import { t } from '@shared/i18n'

/** 当前语言下的应用背景知识（注入 LLM 的 system / 参考段） */
export function appProfile(): string {
  return t('assistant.profile')
}
